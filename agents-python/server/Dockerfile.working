FROM python:3.9-slim

WORKDIR /app

# Copy requirements
COPY requirements_simple.txt requirements.txt

# Install Python dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the server
COPY working_server.py .

EXPOSE 8080

CMD ["python", "working_server.py"]
